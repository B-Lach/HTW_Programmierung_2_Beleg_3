package ui.graphical.keyboard;

/**
 * Enum to represent the action to perform if corresponding keyboard shortcut was triggered
 * @author Benny Lach
 *
 */
public enum KeyboardEnum {
	New,
	NewFromFile,
	Save,
	SaveTo,
	
	AddNode,
	DeleteNode,
	DeleteAll,
}
